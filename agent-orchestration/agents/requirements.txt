# Dependencies for the orchestrator CLI and the FastAPI service.
# The Dockerfile installs this file directly, so keep it to what is imported.

anthropic>=0.40.0
python-dotenv>=1.0.0
fastapi>=0.115.0
uvicorn>=0.32.0
pydantic>=2.0.0
httpx>=0.27.0

# Removed: chromadb and openai. Both were listed but nothing imports them —
# no retrieval is wired, and the orchestrator talks to Anthropic only. Together
# they pulled onnxruntime, tokenizers and numpy into the container image for no
# runtime benefit. Re-add if either is genuinely used.

# For a future framework-based agent loop:
# langgraph>=0.2.0
# langchain-anthropic>=0.3.0
